{
  "name": "side-scrolling-life",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free pixel-art side-scrolling journey.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
